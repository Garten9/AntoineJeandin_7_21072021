create a local database named "groupomania"

run "npm install" from both backend and frontend folder

run "nodemon server" from backend
run "npm run serve" from frontend

website is at http://localhost:8080/
